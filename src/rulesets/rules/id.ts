const Disable = "disable",
      DisableX = "disable-extend",
      EnableX = "enable-extend",
      Enable = "enable",
      Override = "override",
      Recommended = "recommended";

export {
  Disable,
  DisableX,
  EnableX,
  Enable,
  Override,
  Recommended,
};
